import colorVariants, { color } from "@/tokens/schemas/color.css";
import typeVariants, { type } from "@/tokens/schemas/typescale.css";

export default {
  variants: {
    color: colorVariants,
    type: typeVariants,
  },
  tokens: { color, type },
};
