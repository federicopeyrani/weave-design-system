{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "lint": {},
    "lint:fix": {},
    "build": {},
    "react#build": {
      "inputs": [
        "src/**",
        "package.json",
        "tsconfig.json",
        "tsup.config.ts"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "storybook:build": {
      "dependsOn": [
        "^build"
      ],
      "inputs": [
        ".storybook/**",
        "stories/**"
      ],
      "outputs": [
        "storybook-static/**"
      ]
    }
  }
}
